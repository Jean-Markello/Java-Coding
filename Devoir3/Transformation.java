/**
 * An enum class that defines the
 * transformations <b>ID</b> (identity),  
 * <b>ROT</b> (rotation)
 * <b>VSYM</b> (vertical symmetry).
 * and <b>HSYM</b> (horiuzontal symmetry).
 *
 * @author Guy-Vincent Jourdan, University of Ottawa
 */

public enum Transformation {
	ID,
	ROT,
	VSYM,
	HSYM
}